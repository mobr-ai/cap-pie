# cap-pie

CAP is the Cardano Analytics Platform, leveraging LLMs and analytics mechanisms to provide natural language queries, CAP simplifies Cardano data analysis through real-time insights and intuitive, customizable dashboards.

## project structure

Each folder is a git subtree

### backend

This is the CAP subtree mapped to the backend folder (git@github.com:mobr-ai/cap.git repository)

check backend/README.md for details about CAP

cheking backend git logs before subtree mapping: git log ebef786 --

### frontend

This is the CAP Frontend subtree mapped to the frontend folder (git@github.com:mobr-ai/cap-frontend.git repository)

check frontend/README.md for details about CAP frontend

cheking frontend git logs before subtree mapping: git log 78e99db --
